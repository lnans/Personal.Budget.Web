import { Container, Typography } from '@mui/material'

function Settings() {
  return (
    <Container>
      <Typography variant="h4" sx={{ mb: 5 }}>
        Paramètres
      </Typography>
    </Container>
  )
}

export default Settings
