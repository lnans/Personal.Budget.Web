{
  "name": "life-workspaces",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "turbo --ui tui run dev",
    "build": "turbo run build",
    "check": "turbo run check",
    "format": "turbo run format"
  },
  "workspaces": [
    "packages/*"
  ],
  "packageManager": "yarn@4.3.1",
  "devDependencies": {
    "turbo": "^2.0.12"
  }
}
